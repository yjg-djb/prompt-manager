# vibe-coding-pack (split)

本包包含：
- prompts/vibe-coding/：10 个模板（每个模板一个 .md 文件）
- .cursor/rules/：Cursor 项目规则（每条规则一个文件夹 + RULE.md）

用法：
1) 把整个文件夹内容拷贝到你的仓库根目录（或合并进现有结构）。
2) 在 Cursor 中打开仓库后，Rules 会自动生效（alwaysApply / globs / 需要时智能应用）。
3) 模板：复制 prompts/vibe-coding/ 下对应文件内容到对话中使用。
